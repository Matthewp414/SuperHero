@model IEnumerable<RealSuperHeros.Models.Hero>

@{
    ViewBag.Title = "Heros";
}

<h2>Heros</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.AltarEgo)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.PrimaryAbil)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.SecondaryAbil)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.CatchPhrase)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Name)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.AltarEgo)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.PrimaryAbil)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.SecondaryAbil)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.CatchPhrase)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Id }) |
            @Html.ActionLink("Details", "Details", new { id=item.Id }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Id })
        </td>
    </tr>
}

</table>
